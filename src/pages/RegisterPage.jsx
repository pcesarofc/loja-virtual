import { React, RegisterForm } from '../config/configComponents';

const RegisterPage = () => {

    return <main className="register-page">
        <article>
            <section className="section-register">
                <RegisterForm />
            </section>
        </article>
    </main>
}

export default RegisterPage;