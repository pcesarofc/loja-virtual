import { React, LoginForm } from '../config/configComponents';

const LoginPage = () => {

    return <main className="login-page">
        <article>
            <section className="section-login">
                <LoginForm />
            </section>
        </article>
    </main>
}

export default LoginPage;